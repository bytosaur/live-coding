const k = () => new Array(1).fill().reduce(
  (d,b,i)=>(
    d.diff(
      shape(Math.random() + 2)
      .scale(()=>(a.fft[0] > 0.5 ? 0.2 : 0.3))
  //.scroll(Math.random(), Math.sin(time), i/10).color(-1, i/10, Math.random()))
)), solid()
).out()

const bla = 2;
shape(bla).out()

update = 0

// why is div not working

shape(4,0.6).color(1,1,0)
.diff(shape(3,0.5))
.out()


render(o0)
